% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/htest-construct.R
\name{run_htest}
\alias{run_htest}
\title{Eager execution of the tests}
\usage{
run_htest(defs, args, cls, model_id, .data, .name)
}
\description{
This eagerly executes the test
}
\details{
You are allowed to run an H-test function, e.g. through \code{TTEST(extra ~ group, sleep)},
eagerly. Under the hood, \code{defs} contains the list of implementations, construct a dictionary of
implemented functions with \code{build_lookup()}, then match it with \code{find_def()}. The impl. being
look-up is saved as \code{def}, and it's \code{S7} class, not \code{S3} nor \code{S4}.

Since this eagerly executes the test, it won't try to rely on \code{define_model()} to process
the \code{model_id} being defined. It has to be processed directly (thus, \code{processed = model_processor(model_id, data = .data)}
on the third line of code).

Internally, the context of the test is then lookup by \code{infer_context} R6 class, because
this intends to pass on the arguments being used from the implementation. Then, use the
constructed context under \code{def@run()} to execute the test you want to perform.

Save the raw output into a new class, under \code{new_htest()} typically.

The \code{.name} will be the name of the test, and it's optional actually.
}
\keyword{internal}
