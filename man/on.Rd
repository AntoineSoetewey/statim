% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/define-var-ids.R
\name{on}
\alias{on}
\title{Specify variables for independent testing}
\usage{
on(..., .block = NULL)
}
\arguments{
\item{...}{Bare variable names, tidyselect helpers (requires \code{data} in
\code{\link[=define_model]{define_model()}}), or \code{I(expr)} for inline data.}

\item{.block}{Optional. The blocking variable identifying experimental units.
Accepts a bare name, a tidyselect helper, or \code{I(expr)} for inline data.
When supplied, rows are sorted by this variable before injection into the
test function. Required for tests such as "repeated measures ANOVA".}
}
\value{
An \code{on} / \code{var_id} S7 object.
}
\description{
\code{on()} creates an \code{on} Variable Mapper that describes one or more variables
to be tested independently. Expressions are captured unevaluated, similar to
how \code{\link[ggplot2:aes]{ggplot2::aes()}} captures aesthetics.
}
\details{
When multiple variables are supplied, the intended test is run once per
variable with no pairwise combinations formed — for that, use \code{\link[=pairwise]{pairwise()}}.

The optional \code{.block} argument identifies the experimental unit (e.g. a
subject ID column). When supplied, rows in the extracted data are aligned by
block before the test is run — this is required for blocked designs such as
the Friedman test where row position encodes block identity.
}
\examples{
# Single variable
on(x)

# Multiple variables — test on independently
on(a, b, c)

# Multiple variables — With a blocking factor
on(pre, post, followup, .block = subject)

# Tidyselect (requires data in define_model())
on(where(is.numeric))

}
\seealso{
\code{\link[=x_by]{x_by()}}, \code{\link[=rel]{rel()}}, \code{\link[=pairwise]{pairwise()}}, \code{\link[=prop]{prop()}}
}
