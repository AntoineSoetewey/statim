% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/htest-infer-context.R
\name{infer-context-accessors}
\alias{infer-context-accessors}
\alias{ic_pull}
\alias{ic_name}
\alias{ic_arg}
\alias{ic_method_arg}
\alias{ic_claim}
\title{Access data and arguments inside a test implementation}
\usage{
ic_pull(x, role)

ic_name(x, role)

ic_arg(x, name, default = NULL)

ic_method_arg(x, name, default = NULL)

ic_claim(x, name)
}
\arguments{
\item{x}{An \code{infer_context} object passed as \code{self} inside \code{run}.}

\item{role}{A string naming the variable role declared in \code{vars}.
E.g. \code{"x"}, \code{"group"}.}

\item{name}{A string naming the argument or claim.}

\item{default}{A fallback value if the argument was not supplied.}
}
\value{
\itemize{
\item \code{ic_pull()} — a vector
\item \code{ic_name()} — a string
\item \code{ic_arg()} — the argument value or \code{default}
\item \code{ic_method_arg()} — the method argument value or \code{default}
\item \code{ic_claim()} — a \code{ClaimDef} object or \code{NULL}
}
}
\description{
These functions provide access to variables, arguments, and claims
inside the \code{run} function of a \code{test_define()} object. The \code{self}
parameter in \code{run} is an \code{infer_context} object — pass it as \code{x}
to these functions.
}
\examples{
\dontrun{
test_new_def = test_define(
    model_type = "x_by",
    impl_class = "test_new_def_in_two",
    vars = list(
        x = function(p) p$x_data[[1]],
        group = function(p) p$group_data[[1]]
    ),
    run = function(self) {
        grp = as.character(ic_pull(self, "group"))
        resp = ic_pull(self, "x")
        n = ic_method_arg(self, "n")
        ci = ic_arg(self, ".ci", 0.95)
    }
)
}

}
\seealso{
\code{test_define()}, \code{method_spec()}, \code{\link[=via]{via()}}, \code{\link[=conclude]{conclude()}}
}
