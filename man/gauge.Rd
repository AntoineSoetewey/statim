% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/gauge-core.R
\name{gauge}
\alias{gauge}
\title{Effect size for a concluded statistical result}
\usage{
gauge(object, ...)
}
\arguments{
\item{object}{A \code{cld_exec} object produced by \code{conclude()}.}

\item{...}{Passed to the dispatched method.}
}
\value{
A tibble with \code{metric} and \code{value} columns, one row per
effect-size quantity reported by the underlying result class.
}
\description{
\code{gauge()} reports the standardized magnitude of an effect — Cohen's d,
partial eta-squared, odds ratio, and similar quantities — as distinct
from \code{tidy()}'s raw estimates and the p-value's significance verdict.
}
\section{Dispatch}{

Same two paths as \code{tidy()}/\code{glance()}/\code{predict()}:

\strong{Path 1: \code{auto_gauge()} (preferred).}
Called directly when \code{cld_exec@data} is a \code{class_stat_infer} subclass.

\strong{Path 2: \code{making_gauge()} registry (escape hatch).}
Used when a variant's \code{fn} intentionally returns a non-\code{class_stat_infer}
object.
}

\seealso{
\code{\link[=conclude]{conclude()}}, \code{\link[=auto_gauge]{auto_gauge()}}, \code{\link[=making_gauge]{making_gauge()}}
}
