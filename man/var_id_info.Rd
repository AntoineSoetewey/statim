% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/define-var-ids-info.R
\name{var_id_info}
\alias{var_id_info}
\title{Extract metadata from a Variable Mapper}
\usage{
var_id_info(.var_id, processed = NULL, ...)
}
\arguments{
\item{.var_id}{A Variable Mapper object from \code{\link[=x_by]{x_by()}}, \code{\link[=rel]{rel()}}, \code{\link[=pairwise]{pairwise()}},
or a formula.}

\item{processed}{A named list returned by \code{\link[=model_processor]{model_processor()}}, or \code{NULL}.
When \code{NULL}, count-based fields in \code{other_info} and \code{vars} are omitted.}

\item{...}{Currently unused.}
}
\value{
A \code{class_var_inform} S7 object with fields:
\describe{
\item{\code{var_id}}{The original Variable Mapper object.}
\item{\code{model_type}}{Derived from the class name of \code{var_id}.}
\item{\code{args}}{A formatted string summarising the model's arguments.
Defaults to \code{"<?>"} for unregistered subclasses.}
\item{\code{other_info}}{A named list of model-type-specific metadata.
Empty for unregistered subclasses.}
\item{\code{vars}}{A list of lists with \code{name} and \code{preview} fields.
Empty for unregistered subclasses or when \code{processed} is \code{NULL}.}
}
}
\description{
\code{var_id_info()} extracts a consistent metadata structure from a Variable Mapper
object. When \code{processed} is supplied, variable previews and count-based
metadata are included in the result.
}
\examples{
# without processed — no vars, no counts
var_id_info(x_by(extra, group))

# with processed — includes vars and counts
dm = define_model(x_by(extra, group), sleep)
var_id_info(dm@var_id, dm@processed)

}
