% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/htest-ttest.R
\name{TTEST}
\alias{TTEST}
\title{T-Test}
\usage{
TTEST(.var_id = NULL, .data = NULL, ...)
}
\arguments{
\item{.var_id}{A variable mapper \verb{<var_id>} from \code{x_by()}, \code{pairwise()}, or a formula.
When supplied, the test executes immediately.}

\item{.data}{A data frame. Only used on the standalone path.}

\item{...}{Additional arguments passed to the implementation. See the
\strong{Arguments by variable mapper} section for the full list per path.}
}
\value{
A \code{cld_exec} object (in \code{\link[=conclude]{conclude()}}), a \code{stat_infer_spec} object, or a
\code{test_spec} when \code{.var_id = NULL}. Depending on the implementation you wrote, it returns
any class. However, by default, some implementations use base \code{{statim}} S7 classes.
For instance:
\itemize{
\item \code{ttest_x_by}, by default, returns a \link{class_ttest_two} object
\item \code{ttest_pairwise}, by default, returns a \link{class_ttest_pairwise} object
}
}
\description{
\code{TTEST()} performs a t-test for one-sample, two-sample, paired, pairwise, or
formula-based comparisons. If \code{TTEST} is supplied within the lazy-loaded pipeline,
supply \code{TTEST} as a function within i.e. \code{prepare_test(.test = TTEST)} call.
}
\section{Supported variable mapper \verb{<var_id>}s}{

Each variable mapper \verb{<var_id>} routes to a separate implementation. See the linked pages
for full argument lists, variants, and result class details:
\itemize{
\item \code{x_by()}: two-sample or paired t-test. See details from \link{ttest-xby}.
\item \code{pairwise()}: pairwise t-tests across variables. See details from \link{ttest-pairwise}.
\item \verb{<formula>}: one-sample and/or two-sample t-test. See details from \link{ttest-formula}.
}
}

\section{References}{

Welch, B. L. (1947). The generalization of "Student's" problem when
several different population variances are involved. \emph{Biometrika},
34(1-2), 28-35. \url{https://doi.org/10.1093/biomet/34.1-2.28}

Satterthwaite, F. E. (1946). An approximate distribution of estimates
of variance components. \emph{Biometrics Bulletin}, 2(6), 110-114.
\url{https://doi.org/10.2307/3002019}

Kutner, M. H., Nachtsheim, C. J., Neter, J., & Li, W. (2004).
\emph{Applied Linear Statistical Models} (5th ed.). McGraw-Hill/Irwin.
}

\examples{
# eager
TTEST(x_by(extra, group), sleep)

# pipeline
sleep |>
    define_model(x_by(extra, group)) |>
    prepare_test(TTEST) |>
    conclude()

# bootstrap
sleep |>
    define_model(x_by(extra, group)) |>
    prepare_test(TTEST) |>
    via("boot", n = 2000) |>
    conclude()

# permutation
sleep |>
    define_model(x_by(extra, group)) |>
    prepare_test(TTEST) |>
    via("permute", n = 2000) |>
    conclude()

# Contrast t-test
# This uses `state_null()` in a higher degree
# This performs Welch-Satterthwaite linear contrast test for t-test
sleep |>
    define_model(x_by(extra, group)) |>
    prepare_test(TTEST) |>
    state_null(
        2 * MU(extra, group == "1") <= MU(extra, group == "2")
    ) |>
    # Try to obtain 90\% of the confidence interval
    via("contrast", .ci = 0.9) |>
    conclude()

# pairwise
iris |>
    define_model(pairwise(Sepal.Length, Sepal.Width, Petal.Length)) |>
    prepare_test(TTEST) |>
    conclude()

}
\seealso{
\link{ttest-xby}, \link{ttest-pairwise}, \link{ttest-formula} for per-implementation
details. \link{class_ttest_two}, \link{class_ttest_pairwise} for result class
slots. \code{\link[=via]{via()}}, \code{\link[=state_null]{state_null()}}, \code{\link[=conclude]{conclude()}}, \code{\link[=auto_tidy]{auto_tidy()}}.
}
