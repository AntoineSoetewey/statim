% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/model-glm.R
\name{class_glm_object}
\alias{class_glm_object}
\title{Structured result container for GLM fits}
\description{
An S7 class produced by \link{GLM} pipelines. Not constructed manually —
use \code{define_model() |> prepare_model(GLM) |> conclude()} instead.

Inherits from \link{anova_able}, so it participates in \code{\link[=anova]{anova()}} directly.
Downstream packages can use it as a \code{parent} in \code{S7::new_class()}.
}
\details{
Constructor arguments (populated automatically by \link{GLM}):
\itemize{
\item \code{terms}: model terms object.
\item \code{df_residual}: residual degrees of freedom.
\item \code{deviance}: scalar deviance.
\item \code{dispersion}: scalar dispersion parameter.
\item \code{family}: string naming the error family, e.g. \code{"binomial"}.
\item \code{link}: string naming the link function, e.g. \code{"logit"}.
\item \code{null_deviance}: scalar deviance of the intercept-only model.
\item \code{aic}: scalar AIC.
\item \code{logLik}: scalar log-likelihood of the fitted model.
\item \code{null_logLik}: scalar log-likelihood of the intercept-only model.
\item \code{beta}: named numeric vector of coefficient estimates.
\item \code{std_beta}: named numeric vector of coefficient standard errors.
\item \code{fitted}: numeric vector of fitted values on the response scale.
\item \code{vcov}: variance-covariance matrix of the coefficients, e.g.
\code{stats::vcov(fit)}. Required for \code{\link[=predict]{predict()}} with \code{interval}.
\item \code{x_mat}: model matrix stored as a flat numeric vector via
\code{as.numeric(stats::model.matrix(fit))}. Required for \code{\link[=predict]{predict()}}.
\item \code{x_levels}: factor levels used when fitting, via
\code{stats::.getXlevels(fit$terms, stats::model.frame(fit))}. Required
for \code{\link[=predict]{predict()}} on new data with factor predictors.
}

The following are computed automatically and do not need to be supplied:
\itemize{
\item \code{statistic}: per-coefficient test statistics (\code{beta / std_beta}).
\item \code{p_value}: per-coefficient two-sided p-values. Uses a z-test when
\code{family} is \code{"binomial"} or \code{"poisson"} (fixed dispersion), and a
t-test against \code{df_residual} otherwise (estimated dispersion).
\item \code{coefficients}: tibble with columns \code{term}, \code{estimate}, \code{std_error},
\code{statistic}, \code{p_value}.
\item \code{fit_summary}: tibble with columns \code{family}, \code{link}, \code{null_deviance},
\code{deviance}, \code{df_residual}, \code{aic}, \code{n_obs}.
}
}
\section{predict() arguments}{

\code{\link[=predict]{predict()}} on a \code{class_glm_object} accepts:
\itemize{
\item \code{new_data}: A data frame of new predictors. \code{NULL} (the default)
returns fitted values and response-based \code{truth} for the training data.
\item \code{type}: One of \code{"response"} (default, back-transformed through the
inverse link) or \code{"link"} (linear predictor scale).
\item \code{interval}: One of \code{"none"} (default) or \code{"confidence"}.
Prediction intervals are not available, since GLMs have no closed-form
analogue of OLS prediction error.
\item \code{level}: Confidence level for the interval. Default \code{0.95}.
}
}

\examples{
# Inheriting from class_glm_object in a downstream package:
my_glm = S7::new_class(
    "my_glm",
    parent = class_glm_object
)

# Populating class_glm_object from a fitted glm (as done internally):
fit = glm(am ~ wt + hp, data = mtcars, family = binomial())
s = summary(fit)
fam = fit$family$family

obj = class_glm_object(
    terms = fit$terms,
    df_residual = fit$df.residual,
    deviance = fit$deviance,
    dispersion = if (fam \%in\% c("binomial", "poisson")) 1 else s$dispersion,
    family = fam,
    link = fit$family$link,
    null_deviance = fit$null.deviance,
    aic = fit$aic,
    beta = coef(s)[, 1],
    std_beta = coef(s)[, 2],
    fitted = unname(fit$fitted.values),
    vcov = vcov(fit),
    x_mat = as.numeric(model.matrix(fit)),
    x_levels = .getXlevels(fit$terms, model.frame(fit))
)

obj@coefficients
obj@fit_summary

}
\seealso{
\link{anova_able}, \link{GLM}
}
