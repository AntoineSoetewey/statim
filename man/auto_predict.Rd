% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/predict-auto.R
\name{auto_predict}
\alias{auto_predict}
\title{Automatically predict from a statistical result}
\usage{
auto_predict(x, ...)
}
\arguments{
\item{x}{A \code{class_stat_infer} subclass object, typically \code{cld_exec@data}.}

\item{...}{Currently unused. Passed to the dispatched method.}

\item{new_data}{A data frame. \code{NULL} defaults to the training data.}
}
\value{
A data frame with at minimum a \code{.pred} column.
}
\description{
\code{auto_predict()} is the protocol generic for producing predictions from
result objects produced by \code{fn} in \code{baseline()} and \code{variant()}. It is
called automatically by \code{predict()} when the result stored in
\code{cld_exec@data} is a \code{class_stat_infer} subclass.
}
\details{
Register a method on your output class to participate:

\if{html}{\out{<div class="sourceCode r">}}\preformatted{S7::method(auto_predict, my_model_result) = function(x, new_data = NULL, ...) \{
    tibble::tibble(.pred = ...)
\}
}\if{html}{\out{</div>}}

A variant whose \code{fn} returns the same result class as \code{baseline}
inherits \code{auto_predict()} for free via S7's parent chain.
}
\seealso{
\code{\link[=predict]{predict()}}, \code{\link[=making_predict]{making_predict()}}, \code{\link[=method_predict]{method_predict()}}, \link{class_stat_infer}
}
