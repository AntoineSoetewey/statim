% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/stat-prepare.R
\name{prepare}
\alias{prepare}
\title{Prepare a lazy inference pipeline}
\usage{
prepare(.x, .fn, ...)
}
\arguments{
\item{.x}{A \verb{<def_var>} object from \code{\link[=define_model]{define_model()}}, or an
\verb{<expanded_model>} object from \code{\link[=write_models]{write_models()}}.}

\item{.fn}{A stat function built with \code{\link[=STAT_CONSTRUCTOR]{STAT_CONSTRUCTOR()}} that returns
a \verb{<test_spec>} (e.g. \link{TTEST}) or a \verb{<model_spec>} (e.g. \link{LINEAR_REG}).}

\item{...}{Additional arguments passed to the dispatched \verb{prepare_*()} function.}
}
\value{
A \verb{<test_lazy>} object if \code{.fn} returns a \verb{<test_spec>}, or a
\verb{<model_lazy>} object if \code{.fn} returns a \verb{<model_spec>}.
}
\description{
\code{prepare()} attaches a spec function produced from \code{\link[=STAT_CONSTRUCTOR]{STAT_CONSTRUCTOR()}} to a
\verb{<def_var>} object and dispatches to \code{\link[=prepare_test]{prepare_test()}} or \code{\link[=prepare_model]{prepare_model()}}
depending on whether \code{.fn} returns a \verb{<test_spec>} or a \verb{<model_spec>}.
The result is a lazy pipeline object ready for optional recalibration
with \code{\link[=via]{via()}} before execution with \code{\link[=conclude]{conclude()}}.
}
\examples{
sleep |>
    define_model(x_by(extra, group)) |>
    prepare(TTEST) |>
    conclude()

mtcars |>
    define_model(mpg ~ .) |>
    prepare(LINEAR_REG) |>
    conclude()

}
\seealso{
\code{\link[=prepare_test]{prepare_test()}}, \code{\link[=prepare_model]{prepare_model()}}, \code{\link[=define_model]{define_model()}}, \code{\link[=via]{via()}}, \code{\link[=conclude]{conclude()}}
}
