% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/htest-testcor.R
\name{COR_TEST}
\alias{COR_TEST}
\title{Correlation Test}
\usage{
COR_TEST(.var_id = NULL, .data = NULL, ...)
}
\arguments{
\item{.var_id}{A variable mapper \verb{<var_id>} for \code{COR_TEST()}, e.g. \code{\link[=rel]{rel()}}.
When supplied, the test executes immediately.}

\item{.data}{A data frame. Only used on the standalone path.}

\item{...}{Additional arguments passed to the implementation.
See the \strong{Arguments} section of each implementation page.}
}
\value{
A \code{cld_exec} object (in \code{\link[=conclude]{conclude()}}), or a \code{test_spec} object
when \code{.var_id = NULL}. The default correlation test class for most paths is
\link{class_corr_two}.
}
\description{
\code{COR_TEST()} performs a correlation test for one-to-one variable
relationships. If \code{COR_TEST} is supplied within the lazy-loaded pipeline,
supply \code{COR_TEST} as a function i.e. \code{prepare_test(.test = COR_TEST)} call.
}
\section{Supported variable mapper \verb{<var_id>}s}{

Each variable mapper \verb{<var_id>} routes to a separate implementation. See the linked pages
for full argument lists, variants, and correlation test class details:
\itemize{
\item \code{rel()}: one-to-one correlation test. See details from \link{cortest-rel}.
\item \verb{<formula>}: one-to-many correlation test. See details from \link{cortest-formula}.
}
}

\examples{
# eager
COR_TEST(rel(speed, dist), cars)

# grammatical syntax
cars |>
    define_model(rel(speed, dist)) |>
    prepare_test(COR_TEST) |>
    conclude()

cars |>
    define_model(speed ~ dist) |>
    prepare_test(COR_TEST) |>
    conclude()

# Spearman
suppressWarnings({
    cars |>
        define_model(rel(speed, dist)) |>
        prepare_test(COR_TEST) |>
        via("spearman") |>
        conclude()
})

# Custom Hypothesis Expression
cars |>
    define_model(rel(speed, dist)) |>
    prepare_test(COR_TEST) |>
    state_null(RHO(speed, dist) >= 0.8) |>
    conclude()

}
\seealso{
\link{cortest-rel}, \link{cortest-formula} for per-implementation details.
\link{class_corr_two} for correlation test class slots.
\code{\link[=via]{via()}}, \code{\link[=state_null]{state_null()}}, \code{\link[=conclude]{conclude()}}, \code{\link[=auto_tidy]{auto_tidy()}}.
}
