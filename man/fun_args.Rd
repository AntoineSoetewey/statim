% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/htest-construct.R
\name{fun_args}
\alias{fun_args}
\title{Declare arguments for a test implementation}
\usage{
fun_args(...)
}
\arguments{
\item{...}{Named arguments with defaults (\code{name = value}) or one-sided
formulas for required arguments (\code{~name}).}
}
\value{
A \code{fun_args} object — a named list where each element carries
\code{name}, \code{default}, and \code{required} fields.
}
\description{
\code{fun_args()} declares the arguments accepted by a \code{\link[=test_define]{test_define()}} \code{run}
function, along with their default values. Pass the result to the
\code{fun_args} property of \code{\link[=test_define]{test_define()}}.
}
\details{
Arguments are declared in one of two ways:
\itemize{
\item \code{name = value} — argument with a default value
\item \code{~name} — required argument with no default
}

Declared defaults are used by \code{\link[=ic_arg]{ic_arg()}} as fallbacks when the user
does not supply a value. Required arguments cause \code{\link[=ic_arg]{ic_arg()}} to error
if not supplied.
}
\examples{
# all with defaults
fun_args(.paired = TRUE, .mu = 0, .alt = "two.sided", .ci = 0.95)

# mixed — .ci has a default, .paired is required
fun_args(.ci = 0.95, ~.paired)

# used inside test_define()
\dontrun{
new_def = test_define(
    model_type = "x_by",
    impl_class = "new_def_in_two",
    fun_args = fun_args(
        .paired = TRUE,
        .mu = 0,
        .alt = "two.sided",
        .ci = 0.95
    ),
    vars = list(
        x = function(p) p$x_data[[1]],
        group = function(p) p$group_data[[1]]
    ),
    run = function(self) {
        paired = ic_arg(self, ".paired")
        ci = ic_arg(self, ".ci")
    }
)
}

}
\seealso{
\code{\link[=test_define]{test_define()}}, \code{\link[=ic_arg]{ic_arg()}}
}
