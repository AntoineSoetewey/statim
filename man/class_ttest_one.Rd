% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/htest-ttest-one.R
\name{class_ttest_one}
\alias{class_ttest_one}
\title{Structured result container for one-sample t-tests}
\description{
An S7 class produced by \link{T_TEST} pipelines using \code{\link[=on]{on()}} as the variable
mapper \verb{<var_id>}. Not constructed manually — use the pipeline instead.

Inherits from \link{class_stat_infer}, so \code{\link[=auto_tidy]{auto_tidy()}} dispatches on it
automatically. Downstream packages can use it as a \code{parent} in
\code{S7::new_class()}.
}
\details{
Slots (populated automatically by \link{T_TEST}):
\itemize{
\item \code{term}: name of the tested variable.
\item \code{estimate}: sample mean.
\item \code{true_mu}: hypothesized mean as written in the claim. Falls back to
\code{.mu} when no \code{\link[=state_null]{state_null()}} claim is supplied.
\item \code{statistic}: t-statistic.
\item \code{p_val}: p-value.
\item \code{lower_ci}: lower confidence bound.
\item \code{upper_ci}: upper confidence bound.
\item \code{ci_level}: confidence level, e.g. \code{0.95}.
}
}
\section{Shared by variants}{

Both \code{base} and \code{multi} return a \code{class_ttest_one}, so \code{\link[=auto_tidy]{auto_tidy()}} and
\code{\link[=print]{print()}} are inherited by \code{multi} for free.
}

\seealso{
\link{T_TEST}, \link{ttest-on}, \code{\link[=auto_tidy]{auto_tidy()}}, \link{class_stat_infer}
}
