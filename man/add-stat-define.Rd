% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/add-stat-define.R
\name{add-stat-define}
\alias{add-stat-define}
\alias{add_stat_define}
\alias{remove_stat_define}
\title{Add or remove stat_define implementations on a test or model function}
\usage{
add_stat_define(
  stat_fn,
  model_type,
  impl,
  compatible_params = list(),
  origin = c("user", "package"),
  .pkg = NULL
)

remove_stat_define(stat_fn, model_type)
}
\arguments{
\item{stat_fn}{A test or model function built with \code{\link[=HTEST_FN]{HTEST_FN()}} or \code{\link[=MODEL_FN]{MODEL_FN()}}
(e.g. \code{TTEST}, \code{P_TEST}).}

\item{model_type}{An S7 \verb{<var_id>} class (e.g. \code{x_by}, \code{S7::class_formula}).}

\item{impl}{An \code{\link[=agendas]{agendas()}} object.}

\item{compatible_params}{A list of param S7 classes (e.g. \code{list(MU)}).
Defaults to \code{list()}.}

\item{origin}{One of \code{"user"} (default) or \code{"package"}. Use \code{"user"} for
interactive or script-level registration scoped to the current session.
Use \code{"package"} inside your package's \code{.onLoad()}, paired with
\code{.pkg = pkgname} — see the \strong{Package authors} section below.}

\item{.pkg}{The registering package name. Required when \code{origin = "package"};
ignored otherwise. Pass the \code{pkgname} argument that R supplies to
\code{.onLoad()} / \code{.onAttach()}. This is used to attribute the entry and to enable
\code{\link[=purge_stat_defines]{purge_stat_defines()}} to clean it up on unload.}
}
\value{
\code{NULL}, invisibly.
}
\description{
These are developer-interface functions intended for package authors
extending the \code{statim} framework with new model types.

\code{add_stat_define()} registers a new \code{\link[=stat_define]{stat_define()}} for a stat function,
enabling it to handle a previously unsupported variable mapper \verb{<var_id>}.
Registering a model type that already exists — whether baked-in or
previously registered — is an error.

\code{remove_stat_define()} removes a previously registered \code{"user"}-originated
entry. \code{"package"}-scoped entries are self-cleaning via \code{\link[=purge_stat_defines]{purge_stat_defines()}}
called in the registering package's \code{.onUnload()}.
}
\section{Scoping and lifecycle}{


Registrations have two scopes:
\itemize{
\item \emph{\code{"user"}-scoped} entries live for the duration of the R session (or
until explicitly removed with \code{\link[=remove_stat_define]{remove_stat_define()}}). Use this for
interactive work or in scripts.
\item \emph{\code{"package"}-scoped} entries are intended for package authors who ship
extensions to \code{statim}. They must be registered in \code{.onLoad()} and
cleaned up in \code{.onUnload()} via \code{\link[=purge_stat_defines]{purge_stat_defines()}}. This keeps the
registry tidy when the extending package is unloaded.
}
}

\section{Package authors}{


To ship an extension as a package, register in \code{zzz.R} (or any file loaded
early):

\if{html}{\out{<div class="sourceCode r">}}\preformatted{.onLoad = function(libname, pkgname) \{
    statim::add_stat_define(
        P_TEST,
        my_var_id,
        impl = agendas(
            base = baseline(
                fn = function(.proc, .p = 0.5, .alt = "two.sided", .ci = 0.95) \{
                    # your implementation
                \}
            )
        ),
        compatible_params = list(PI),
        origin = "package",
        .pkg = pkgname
    )
\}

.onUnload = function(libpath) \{
    statim::purge_stat_defines("yourpackage")
\}
}\if{html}{\out{</div>}}

The \code{.pkg = pkgname} argument is how \code{statim} knows which package owns
the entry. Without it, \code{origin = "package"} is an error. Never hard-code
the string yourself — always forward the \code{pkgname} R passes to \code{.onLoad()}.
}

\examples{
# Session-scoped registration (interactive use or scripts)
mt = S7::new_class("my_var", parent = var_id)

add_stat_define(
    P_TEST,
    mt,
    impl = agendas(
        base = baseline(
            fn = function(.proc, .value = 1) list(value = .value)
        )
    )
)

# Clean up when done
remove_stat_define(P_TEST, mt)

}
\seealso{
\code{\link[=stat_define]{stat_define()}}, \code{\link[=agendas]{agendas()}}, \code{\link[=remove_stat_define]{remove_stat_define()}},
\code{\link[=purge_stat_defines]{purge_stat_defines()}}
}
