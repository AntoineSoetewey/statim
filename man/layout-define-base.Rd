% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/define-model.R
\name{layout-define-base}
\alias{layout-define-base}
\alias{define_model}
\title{Define a layout supplied by a Variable Mapper}
\usage{
define_model(.x, ...)
}
\arguments{
\item{.x}{A variable mapper \verb{<var_id>} object from \code{\link[=x_by]{x_by()}}, \code{\link[=rel]{rel()}}, \code{\link[=pairwise]{pairwise()}}, or a
formula. It is also dispatched for a data frame class when using the data-first
pipe style.}

\item{...}{Currently unused.}
}
\value{
A \code{def_var} S3 object containing \code{var_id} and \code{processed}.
}
\description{
\code{define_model()} captures a variable mapper \verb{<var_id>} and optional data into a \code{def_var}
object that can be passed into \code{\link[=prepare_test]{prepare_test()}}.
}
\details{
Two dispatch methods are available depending on how \code{.x} is supplied:
\itemize{
\item \strong{A "Variable Mapper" first}: \code{.x} is a Variable Mapper or formula. Accepts \code{data}, a
data frame (defaults to \code{parent.frame()}).
\item \strong{DataFrame-first}: \code{.x} is a data frame. Accepts \code{to_analyze}, a variable mapper
or formula, as the second argument.
}
}
\examples{
# model-ID first
define_model(x_by(extra, group), sleep)

# data-frame first (pipe-friendly)
sleep |> define_model(x_by(extra, group))

}
