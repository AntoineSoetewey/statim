% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/htest-testcor-defs-formula.R
\name{cortest-formula}
\alias{cortest-formula}
\title{Correlation Test: Formula interface}
\description{
The formula implementation performs pairwise correlation tests between a
single response variable (LHS) and one or more independent variables (RHS).
\enumerate{
\item \code{y ~ x}: one independent variable, one correlation test.
\item \code{y ~ x1 + x2}: multiple independent variables, one test per RHS term.
}

Use a formula directly as the model ID to select this implementation.
}
\section{Arguments}{

The following arguments are passed via \code{...} in \code{\link[=COR_TEST]{COR_TEST()}}:

\describe{
\item{\code{.cor_type}}{String. One of \code{"pearson"}, \code{"spearman"}, or
\code{"kendall"}. Default \code{"pearson"}.}
\item{\code{.alt}}{String. One of \code{"two.sided"}, \code{"greater"}, or \code{"less"}.
Default \code{"two.sided"}.}
\item{\code{.ci}}{Numeric. Confidence level. Default \code{0.95}. Only used for
Pearson; silently ignored for Kendall and Spearman.}
}
}

\section{Correlation test default class}{

As detailed by \link{cortest-rel}, it returns a \link{class_corr_two} object inheriting
from \link{class_stat_infer} by default. You need to process outputs by:
\itemize{
\item \code{print()}: Write it down through \code{print} from \code{\link[=variant]{variant()}}.
\item \code{tidy()}: Use \code{\link[=making_tidy]{making_tidy()}} to register a tidy method if needed.
}

if the variants from this method pipeline doesn't return a \link{class_corr_two} object.
}

\section{Hypothesis claims}{

Not supported. Use \code{\link[=rel]{rel()}} with the \code{base} variant for \code{\link[=state_null]{state_null()}}
with \code{\link[=RHO]{RHO()}}.
}

\examples{
cars |>
    define_model(dist ~ speed) |>
    prepare_test(COR_TEST) |>
    conclude()

# multiple independent variables
mtcars |>
    define_model(mpg ~ wt + hp) |>
    prepare_test(COR_TEST) |>
    conclude()

}
\seealso{
Other cortest-implementations: 
\code{\link{cortest-rel}}
}
\concept{cortest-implementations}
\keyword{internal}
