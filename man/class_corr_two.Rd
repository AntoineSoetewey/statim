% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/htest-testcor.R
\name{class_corr_two}
\alias{class_corr_two}
\title{Structured result container for two-sample t-tests}
\description{
An S7 class produced by \link{CORTEST} using \code{\link[=rel]{rel()}} and \verb{<formula>} as the model ID.
Not constructed manually, use the "grammar interface" instead.

Inherits from \link{class_stat_infer}, so \code{\link[=auto_tidy]{auto_tidy()}} dispatches on it
automatically. Downstream packages can use it as a \code{parent} in
\code{S7::new_class()}.
}
\details{
Slots (populated automatically by \link{CORTEST}):
\itemize{
\item \code{ind_vars}: name of the independent variables.
\item \code{resp_vars}: name of the response / dependent variables.
\item \code{estimate}: the estimated correlation coefficient.
\item \code{statistic}: t-statistic.
\item \code{df}: degrees of freedom.
\item \code{p_val}: p-value.
\item \code{lower_ci}: lower confidence bound.
\item \code{upper_ci}: upper confidence bound.
\item \code{ci_level}: confidence level, e.g. \code{0.95}.
}
}
\section{Shared by variants}{

Both \code{\link[=rel]{rel()}} and \verb{<formula>}'s default (\code{base}) return a \code{class_corr_two}, so different
models shares both \code{\link[=auto_tidy]{auto_tidy()}} and \code{\link[=print]{print()}} for free.
}

\seealso{
\link{CORTEST}, \code{\link[=auto_tidy]{auto_tidy()}}, \link{class_stat_infer}
}
