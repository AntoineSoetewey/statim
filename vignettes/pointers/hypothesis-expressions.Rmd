---
title: "Writing Full Null Hypothesis Expression"
subtitle: "A New Feature and Explanation"
output: 
    rmarkdown::html_vignette:
        toc: true
vignette: |
    %\VignetteIndexEntry{Hypothesis Expressions}
    %\VignetteEngine{knitr::rmarkdown}
    \usepackage[utf8]{inputenc}
---

```{r, include = FALSE}
knitr::opts_chunk$set(
    collapse = TRUE,
    comment = "#>"
)
library(statim)
```

## Rationale

It may sound complex to you, but it is intuitive and logical. `{statim}` tries to emulate the way the null hypotheses being written under the statistics textbooks you read. Under the hood, it signifies the use of syntactic sugars and DSLs (domain specific language). It was inspired by `join_by()` from `{dplyr}` package, where... 

## 

##

## Potential Grips

It tries to be strict when writing hypothesis expressions, as it tries to be mathematically sound. 



